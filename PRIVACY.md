# 隐私说明

Zotero Library Check 的设计目标是让文献匹配在用户本机完成。

## 处理的数据

扩展会读取：

- 本机 Zotero 个人文库中的条目键、标题、DOI、年份、第一作者、出版物名称等匹配所需元数据。
- Web of Science 当前页面中可见论文的标题、DOI、年份和作者信息。

## 数据用途与存储

这些数据仅用于判断 WOS 论文是否已经存在于 Zotero 文库。精简后的 Zotero 索引存储在扩展自己的浏览器 IndexedDB 中；同步状态存储在 `chrome.storage.local` 中。

## 网络通信

- 扩展仅通过 `127.0.0.1:23119` 或 `localhost:23119` 访问用户本机的 Zotero Local API。
- 扩展不会把 Zotero 文库数据、WOS 页面数据或匹配结果发送到开发者或其他远程服务器。
- 扩展不包含分析、广告或遥测代码。

## 写入行为

对 Zotero Local API 的访问是只读的。扩展不会创建、修改或删除 Zotero 条目，也不会自动导入论文。

## 删除数据

从 Chrome 中移除扩展会清除该扩展的本地浏览器存储。此操作不会影响 Zotero 文库。用户也可以通过 Chrome 的扩展数据管理功能清除本地数据。

## 说明范围

本说明适用于仓库中未修改的官方构建。第三方修改版本可能具有不同的数据处理行为。
